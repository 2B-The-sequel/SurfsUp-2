@model SurfsUp.Models.Equipment

@{
    ViewData["Title"] = "Detaljer";
}

<h1>Detaljer</h1>

<div>
    <h4>Udstyr</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Adminstrators"))
    {
        <a asp-action="Edit" asp-route-id="@Model?.EquipmentId" class="btn btn-success">Ændr</a>
    }
    <a asp-action="Index" class="btn btn-primary">Tilbage til listen</a>
</div>
